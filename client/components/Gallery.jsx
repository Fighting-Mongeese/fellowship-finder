import React from 'react';
import axios from 'axios';

const Gallery = (props) => {
  const { user } = props;

  const getUserPhotos = () => {
    
  }
  return (
    <div>
      Gallery
    </div>
  );
};

export default Gallery;
